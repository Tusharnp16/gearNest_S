package com.example.gearnest.repository;

import com.example.gearnest.model.GarageProfile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GarageProfileRepository extends JpaRepository<GarageProfile, Long> {}